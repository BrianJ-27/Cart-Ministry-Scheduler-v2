import React from "react";
import Cards from "../reusable/cards/Cards";

const Dashboard = () => {
  return (
    <main className="main">
      <Cards />;
    </main>
  );
};

export default Dashboard;
